import 'dart:typed_data';

import './verifier.dart';

class IpfsVerifier implements IVerifier {
  final proxyUrl = 'https://ipfs.io/ipfs/';

  @override
  Future<bool> verify(String domain) {
    
  }
}